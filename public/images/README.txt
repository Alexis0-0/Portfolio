Place these files here:

profile.jpg      -> your headshot / profile photo (used in the About section)
project-1.jpg    -> screenshot of the TAU E-Commerce project (used in Projects section)
og-cover.jpg     -> 1200x630 social preview image (used for Open Graph / Twitter cards)

If a file is missing, the site automatically shows a graceful text
fallback instead of a broken image icon, so the site will still work
without these — just less polished.
